import assert from 'assert';
// import {name as appName} from './client/app.jsx';

/*
describe('quitr', () => {
  it('should export its name', () => {
    assert.strictEqual(appName, 'quitr');
  });
});
*/
